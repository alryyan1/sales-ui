// src/App.tsx
import React from 'react';
// يمكنك إزالة هذا إذا لم تعد تستخدمه بعد تطبيق MUI CssBaseline
// import './App.css';

/**
 * مكون App (دور محدود مع createBrowserRouter)
 *
 * في الإعدادات التي تستخدم `createBrowserRouter` (React Router v6.4+)،
 * يتم عادةً التعامل مع هيكل التطبيق الرئيسي والتوجيه والتخطيط
 * مباشرةً داخل تكوين جهاز التوجيه (على سبيل المثال، في main.tsx أو router.tsx)
 * ومكون التخطيط الجذري المحدد هناك (على سبيل المثال، RootLayout.tsx).
 *
 * غالبًا ما يصبح مكون App.tsx هذا بسيطًا أو حتى غير ضروري.
 * لم يعد المكان الذي يتم فيه تحديد <Routes>.
 *
 * يمكنك إما:
 * 1. إزالة هذا الملف وتعديل الاستيرادات في main.tsx إذا لزم الأمر.
 * 2. الاحتفاظ به كعنصر نائب أساسي جدًا إذا كان مطلوبًا بواسطة بعض الأدوات.
 * 3. نقل محتوى RootLayout.tsx إلى هنا إذا كنت تفضل اسم "App.tsx"
 *    لمكون التخطيط الرئيسي الخاص بك.
 *
 * بالنسبة لهيكلنا الحالي، يعمل RootLayout.tsx بمثابة غلاف التطبيق الرئيسي.
 */
function App() {
  // هذا المكون لا يعرض أي شيء تتم إدارته مباشرة بواسطة RouterProvider.
  // يتم تحديد واجهة المستخدم الفعلية من خلال تكوين جهاز التوجيه والعناصر
  // المحددة هناك (مثل RootLayout، HomePage، LoginPage، إلخ).
  return (
     <>
       {/*
         يتم عرض المحتوى عبر <RouterProvider> في main.tsx بناءً على
         تكوين المسار، والذي يستخدم <RootLayout> كغلاف رئيسي.
         لا يحتاج هذا المكون نفسه إلى عرض أي شيء هنا.
       */}
       {/* يمكنك ترك هذا فارغًا أو إزالته */}
     </>
  );
}

export default App;